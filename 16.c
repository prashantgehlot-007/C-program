/* no is even or odd*/
#include<stdio.h>
#include<conio.h>
int main(int argc, char const *argv[])
{
    int a;
    printf("enter the num : ");
    scanf("%d",&a);
    if(a/2==0)
    printf("num is even %d",a);
    else
    printf("num is odd %d",a);
    return 0;
}

/* same for no. is divisible ny any no.*/
